{% extends 'administration/index.html.twig' %}

{% block title %}Crear carta{% endblock %}

{% block styles %}
    {{ parent() }}

    <link rel="stylesheet" type="text/css" href="{{ asset('/assets/styles/form.css') }}" />
{% endblock %}

{% block main %}
    {% include '_back-button.html.twig' %}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {% for field in form %}
            {% if field.vars.name != '_token' %}
                <fieldset class="form-group">
                    {{ form_label(field) }}
                    {{ form_widget(field) }}
                    {{ form_errors(field) }}
                </fieldset>
            {% endif %}
        {% endfor %}

        {{ form_widget(form._token) }}

        <input type="submit" value="Crear">
    {{ form_end(form) }}

{% endblock %}

{% block scripts %}
    {{ parent() }}

    <script src="{{ asset('components/card-validation.js') }}"></script>
{% endblock %}
