/**
 * Los imports se hacen desde un importmap en index.html.
 * El objetivo es separarlo en un archivo aparte para no tener cada importación definida directamente en el HTML.
 */

import './components/card.js';
